# AI Update Tracker

Purpose: track high-signal repo updates that change how AI agents and humans should understand this workspace.

- 2026-04-12: Imported the `VWA AIRPORT` and `guttenburg` Codex workflow layer into `TADOI`, including repo-local `AGENTS.md`, `docs/ai` durable memory, `docs/codex` task-memory scaffolding, vendored skill snapshots under `ai/skills/`, and repo-owned workflow scripts under `scripts/codex/`.
