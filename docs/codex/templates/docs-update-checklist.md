# Docs Update Checklist

- Confirm whether `README.md` changed scope, commands, or setup instructions.
- Confirm whether `AGENTS.md` or `docs/CODEX_WORKFLOW.md` changed due to workflow updates.
- Confirm whether `docs/ai/*` needs a factual update for durable repo memory.
- Confirm whether only high-signal changes were added to `docs/ai/05-update-tracker.md`.
- Confirm whether task/session notes belong in `docs/codex/tasks/**` instead of a new ad hoc file.
