name: Daily Build

on:
  schedule:
    - cron: "0 14 * * *" # 08:00 America/Chicago (UTC; DST shift applies)
  workflow_dispatch:
    inputs:
      upload_artifacts:
        description: "Upload artifacts (recommended for manual runs only)"
        required: false
        default: true
        type: boolean

jobs:
  daily-build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux
          - os: macos-latest
            target: macos
          - os: windows-latest
            target: windows
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76 # v2.0.2
        with:
          bun-version: 1.3.9

      - name: Bun version
        run: bun --version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Install Inno Setup (Windows)
        if: matrix.target == 'windows'
        run: choco install innosetup --no-progress -y

      - name: Install Linux packaging prerequisites
        if: matrix.target == 'linux'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y dpkg-dev

          if ! command -v dpkg-deb >/dev/null 2>&1; then
            echo "dpkg-deb not found after installing dpkg-dev." >&2
            exit 1
          fi

          appimagetool_version="1.9.1"
          appimage_path="$RUNNER_TEMP/appimagetool-${appimagetool_version}.AppImage"
          wrapper_path="$RUNNER_TEMP/appimagetool"
          curl -fsSL "https://github.com/AppImage/appimagetool/releases/download/${appimagetool_version}/appimagetool-x86_64.AppImage" -o "$appimage_path"
          chmod +x "$appimage_path"
          {
            echo "#!/usr/bin/env bash"
            echo "set -euo pipefail"
            echo "APPIMAGE_EXTRACT_AND_RUN=1 \"$appimage_path\" \"\$@\""
          } > "$wrapper_path"
          chmod +x "$wrapper_path"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"
          export PATH="$RUNNER_TEMP:$PATH"

          if ! command -v appimagetool >/dev/null 2>&1; then
            echo "appimagetool wrapper install failed." >&2
            exit 1
          fi

      - name: Build daily artifacts
        run: bun run build:daily

      - name: Assert Windows installer output
        if: matrix.target == 'windows'
        shell: pwsh
        run: |
          $matches = @(Get-ChildItem -Path "dist/artifacts" -Recurse -File -Filter "TADOI-Setup-x64-*.exe" -ErrorAction SilentlyContinue)
          if ($matches.Count -lt 1) {
            Write-Error "Expected Windows installer (TADOI-Setup-x64-*.exe) under dist/artifacts, but none was found."
            exit 1
          }

          Write-Host "Found Windows installer artifact(s):"
          $matches | ForEach-Object { Write-Host " - $($_.FullName)" }

      - name: Upload artifacts
        if: always() && github.event_name == 'workflow_dispatch' && inputs.upload_artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: tadoi-daily-${{ matrix.target }}-${{ github.sha }}
          retention-days: 2
          if-no-files-found: error
          path: |
            dist/artifacts/**/BUILD_REPORT.md
            dist/artifacts/**/${{ matrix.target }}/**
